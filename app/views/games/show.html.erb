<h1>Attempts remaining: <%= @attempts_remaining %> </h1>

<div class='container-fluid'>
  <%= form_with model: @current_game, class: 'col-lg-3' do |f| %>
    <% if @current_game.errors.any? %>
      <h2><%= pluralize(@current_game.errors.count, "error") %> prohibited this Resource from being saved:</h2>
      <ul>
      <% @current_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    <% end %>
    <div class='form-group'>
      <%= f.label :user_guess, 'Enter number' %>
      <%= f.text_field :user_guess, placeholder: '1234', class: 'form-control', required: true %>
    </div>
      <%= f.submit "GUESS!", class: "btn btn-success" %>
  <% end %>
</div>
